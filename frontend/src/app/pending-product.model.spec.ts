import { PendingProductList } from './pending-product.model';

describe('PendingProductList', () => {
  it('should create an instance', () => {
    expect(new PendingProductList()).toBeTruthy();
  });
});
